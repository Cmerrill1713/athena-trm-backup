Title: I was using Claude Code wrong... The Ultimate Workflow
Channel: AI Jason
======================================================================

For the past few weeks, I have For the past few weeks, I have For the past few weeks, I have completely switched from cursor to cloud completely switched from cursor to cloud completely switched from cursor to cloud code and learned a ton of things that code and learned a ton of things that code and learned a ton of things that made my cloud code super effective. And made my cloud code super effective. And made my cloud code super effective. And today, I will take you through how do I today, I will take you through how do I today, I will take you through how do I use cloud code, how did I bring hero use cloud code, how did I bring hero use cloud code, how did I bring hero type of specdriven demand process as type of specdriven demand process as type of specdriven demand process as well as tips and practical examples of well as tips and practical examples of well as tips and practical examples of how I use feature like hooks, custom how I use feature like hooks, custom how I use feature like hooks, custom commands, and list of tips that made commands, and list of tips that made commands, and list of tips that made myself much more effective. So, without myself much more effective. So, without myself much more effective. So, without further ado, let's get started. But further ado, let's get started. But further ado, let's get started. But before we dive into that, I know many of before we dive into that, I know many of before we dive into that, I know many of you are just getting started learning you are just getting started learning you are just getting started learning programming. And one question I got programming. And one question I got programming. And one question I got asked a lot is what does road map look asked a lot is what does road map look asked a lot is what does road map look like for learning code effectively. like for learning code effectively. like for learning code effectively. That's why I want to introduce you to That's why I want to introduce you to That's why I want to introduce you to this free ebook made by Google's this free ebook made by Google's this free ebook made by Google's principal analytics lead and data principal analytics lead and data principal analytics lead and data scientist where she wrote down all the scientist where she wrote down all the scientist where she wrote down all the secret tips and methodologies that she secret tips and methodologies that she secret tips and methodologies that she used to learn coding with claw and chatb used to learn coding with claw and chatb used to learn coding with claw and chatb especially how she get output especially how she get output especially how she get output personalized learning road map based on personalized learning road map based on personalized learning road map based on her specific situation. It also cover her specific situation. It also cover her specific situation. It also cover all the absolute fundamentals and basics all the absolute fundamentals and basics all the absolute fundamentals and basics of coding like how do you choose the of coding like how do you choose the of coding like how do you choose the right coding language to start with and right coding language to start with and right coding language to start with and best practice prompt for different best practice prompt for different best practice prompt for different coding scenario like debugging and coding scenario like debugging and coding scenario like debugging and optimizing the code as well detailed optimizing the code as well detailed optimizing the code as well detailed road map of how to master language like road map of how to master language like road map of how to master language like Python in just four months. There's even Python in just four months. There's even Python in just four months. There's even a custom GPT that has baking all the a custom GPT that has baking all the a custom GPT that has baking all the core knowledge of latest package and core knowledge of latest package and core knowledge of latest package and learning resource that you can talk to learning resource that you can talk to learning resource that you can talk to alongside detailed video tutorial alongside detailed video tutorial alongside detailed video tutorial showcasing her step-by-step work. So, I showcasing her step-by-step work. So, I showcasing her step-by-step work. So, I definitely recommend you go take a look definitely recommend you go take a look definitely recommend you go take a look if you're just getting started with your if you're just getting started with your if you're just getting started with your coding journey. I put a link in the coding journey. I put a link in the coding journey. I put a link in the description below for you to download description below for you to download description below for you to download for free and thanks HubSpot for sharing for free and thanks HubSpot for sharing for free and thanks HubSpot for sharing this awesome material with us. Now, this awesome material with us. Now, this awesome material with us. Now, let's get back to how do I use cloud let's get back to how do I use cloud let's get back to how do I use cloud code. So, first thing first, you want to code. So, first thing first, you want to code. So, first thing first, you want to install this cloud code extension and install this cloud code extension and install this cloud code extension and this will allow you to deeply integrate this will allow you to deeply integrate this will allow you to deeply integrate into your current ID like cursor, VS into your current ID like cursor, VS into your current ID like cursor, VS code or wingserf. So now I can click on code or wingserf. So now I can click on code or wingserf. So now I can click on this run cloud code button and it will this run cloud code button and it will this run cloud code button and it will automatically open cloud code in my automatically open cloud code in my automatically open cloud code in my current ID and on the bottom right you current ID and on the bottom right you current ID and on the bottom right you should see that it is detecting the should see that it is detecting the should see that it is detecting the specific file I'm in and if I select specific file I'm in and if I select specific file I'm in and if I select some lines it will also automatically some lines it will also automatically some lines it will also automatically detect it. You can still use the detect it. You can still use the detect it. You can still use the terminal here as well and you can just terminal here as well and you can just terminal here as well and you can just do / IDE to choose the specific ID that do / IDE to choose the specific ID that do / IDE to choose the specific ID that you wanted to connect to. Next the first you wanted to connect to. Next the first you wanted to connect to. Next the first thing before you do anything is that you thing before you do anything is that you thing before you do anything is that you should run this init command line. So should run this init command line. So should run this init command line. So what this in command line do is that it what this in command line do is that it what this in command line do is that it will get cloud start analyzing your will get cloud start analyzing your will get cloud start analyzing your codebase to learn about what already set codebase to learn about what already set codebase to learn about what already set up what are the kind of dependencies up what are the kind of dependencies up what are the kind of dependencies components that you should be using. It components that you should be using. It components that you should be using. It will also ask your permission to do will also ask your permission to do will also ask your permission to do something for those type of commands something for those type of commands something for those type of commands like cd I just don't care I will just like cd I just don't care I will just like cd I just don't care I will just click on always auto approve and you click on always auto approve and you click on always auto approve and you will see this thing is automatically will see this thing is automatically will see this thing is automatically saved as permissions. I know some of you saved as permissions. I know some of you saved as permissions. I know some of you actually want to get cloud code always actually want to get cloud code always actually want to get cloud code always automatically run tools and there's a automatically run tools and there's a automatically run tools and there's a way you can run that as well. You can do way you can run that as well. You can do way you can run that as well. You can do this dangerously skip permissions and this dangerously skip permissions and this dangerously skip permissions and this will show you that it is bypassing this will show you that it is bypassing this will show you that it is bypassing all the permissions. So now tool it will all the permissions. So now tool it will all the permissions. So now tool it will ask you for permission though I wouldn't ask you for permission though I wouldn't ask you for permission though I wouldn't actually recommend this because from my actually recommend this because from my actually recommend this because from my experience one of the key advantage of experience one of the key advantage of experience one of the key advantage of the cloud codes UX is that allow you to the cloud codes UX is that allow you to the cloud codes UX is that allow you to interact with cloud much better and I interact with cloud much better and I interact with cloud much better and I will explain what I mean. All right so will explain what I mean. All right so will explain what I mean. All right so after it scan finish it will try to after it scan finish it will try to after it scan finish it will try to create this cloud MD file. If you're create this cloud MD file. If you're create this cloud MD file. If you're using cursor, that's basically your using cursor, that's basically your using cursor, that's basically your cursor rules. And if we click inside, cursor rules. And if we click inside, cursor rules. And if we click inside, you can see that it automatically detect you can see that it automatically detect you can see that it automatically detect what are the kind of tax stack, what are what are the kind of tax stack, what are what are the kind of tax stack, what are the ROM build commands, the the ROM build commands, the the ROM build commands, the architecture, the project structure, all architecture, the project structure, all architecture, the project structure, all the useful stuff. So this give cloud a the useful stuff. So this give cloud a the useful stuff. So this give cloud a very good base to continue developing very good base to continue developing very good base to continue developing new things on top of this ripple. And new things on top of this ripple. And new things on top of this ripple. And this cloud MD as I mentioned before is this cloud MD as I mentioned before is this cloud MD as I mentioned before is pretty much the cursor rules. The only pretty much the cursor rules. The only pretty much the cursor rules. The only difference is that in cursor the rules difference is that in cursor the rules difference is that in cursor the rules are injected more programmatically. But are injected more programmatically. But are injected more programmatically. But for cloud is much easier. Basically for cloud is much easier. Basically for cloud is much easier. Basically every single run this text you define every single run this text you define every single run this text you define here is appended on top of system here is appended on top of system here is appended on top of system message and I know there are a lot of message and I know there are a lot of message and I know there are a lot of people have different sorts of like a people have different sorts of like a people have different sorts of like a cursor rules that to enhance the cursor rules that to enhance the cursor rules that to enhance the workflow. For me it normally is pretty workflow. For me it normally is pretty workflow. For me it normally is pretty simple. One prom I do add in is this simple. One prom I do add in is this simple. One prom I do add in is this plan and review mode. This basically plan and review mode. This basically plan and review mode. This basically tell cloud code that before we start tell cloud code that before we start tell cloud code that before we start working on the project always in the working on the project always in the working on the project always in the plan mode make a plan first. And for the plan mode make a plan first. And for the plan mode make a plan first. And for the plan we want to save to this docloud plan we want to save to this docloud plan we want to save to this docloud /task/taskname.md /task/taskname.md /task/taskname.md so that we can keep track that later. so that we can keep track that later. so that we can keep track that later. And inside this task, we should kind of And inside this task, we should kind of And inside this task, we should kind of break down into different tasks. While break down into different tasks. While break down into different tasks. While cloud code is doing its job, it should cloud code is doing its job, it should cloud code is doing its job, it should also update the plan as we go and append also update the plan as we go and append also update the plan as we go and append what it does into the doc. And this is what it does into the doc. And this is what it does into the doc. And this is really useful because it is almost really useful because it is almost really useful because it is almost always better to just align with cloud always better to just align with cloud always better to just align with cloud code what exactly do you want. And this code what exactly do you want. And this code what exactly do you want. And this kind of also similar to Amazon's new uh kind of also similar to Amazon's new uh kind of also similar to Amazon's new uh kro workflow as well where they call it kro workflow as well where they call it kro workflow as well where they call it like spec driven development. And what like spec driven development. And what like spec driven development. And what it does is kind of similar. Uh it will it does is kind of similar. Uh it will it does is kind of similar. Uh it will ask you to go through the requirements ask you to go through the requirements ask you to go through the requirements and also kind of go through the and also kind of go through the and also kind of go through the architecture design come up. It's architecture design come up. It's architecture design come up. It's basically same thing and obviously you basically same thing and obviously you basically same thing and obviously you can go as deep as like ko where you will can go as deep as like ko where you will can go as deep as like ko where you will break down this three steps process but break down this three steps process but break down this three steps process but I often found it's effective enough to I often found it's effective enough to I often found it's effective enough to just do one kind of prd. So I will save just do one kind of prd. So I will save just do one kind of prd. So I will save this and now we can get cloud code to this and now we can get cloud code to this and now we can get cloud code to start implementing our feature. Normally start implementing our feature. Normally start implementing our feature. Normally the first thing I would do is make a the first thing I would do is make a the first thing I would do is make a plan. So our give prompt we are building plan. So our give prompt we are building plan. So our give prompt we are building a beautiful online ID front end. help me a beautiful online ID front end. help me a beautiful online ID front end. help me break down into key components and put break down into key components and put break down into key components and put them together in the end. And here I'm them together in the end. And here I'm them together in the end. And here I'm going to do shift tab. It will firstly going to do shift tab. It will firstly going to do shift tab. It will firstly get into this auto accept edits mode, get into this auto accept edits mode, get into this auto accept edits mode, but you want to shift tab again into but you want to shift tab again into but you want to shift tab again into plan mode. So this plan mode is a really plan mode. So this plan mode is a really plan mode. So this plan mode is a really really useful feature. When the agent is really useful feature. When the agent is really useful feature. When the agent is in plan mode, it has this special system in plan mode, it has this special system in plan mode, it has this special system prompt and limit access to tools. So it prompt and limit access to tools. So it prompt and limit access to tools. So it will focus on doing things like web will focus on doing things like web will focus on doing things like web search to understand the latest like tax search to understand the latest like tax search to understand the latest like tax stack or documentations and also stack or documentations and also stack or documentations and also planning the architecture and in the end planning the architecture and in the end planning the architecture and in the end it will generate final reports and this it will generate final reports and this it will generate final reports and this planning sometimes will take a while for planning sometimes will take a while for planning sometimes will take a while for any feature that is semi complicated. I any feature that is semi complicated. I any feature that is semi complicated. I would always do this plan mode first would always do this plan mode first would always do this plan mode first back and forth a few times to align the back and forth a few times to align the back and forth a few times to align the plan with cloud code and only after I plan with cloud code and only after I plan with cloud code and only after I getting it start implementing the getting it start implementing the getting it start implementing the feature. So here you will see that it is feature. So here you will see that it is feature. So here you will see that it is showing task. When you see task it showing task. When you see task it showing task. When you see task it basically means cloud code is calling a basically means cloud code is calling a basically means cloud code is calling a sub aents that is specifically doing sub aents that is specifically doing sub aents that is specifically doing this planning and research. So at this planning and research. So at this planning and research. So at default cloud code has 17 different default cloud code has 17 different default cloud code has 17 different tools that can do things like run tools that can do things like run tools that can do things like run command line, read and find files, file command line, read and find files, file command line, read and find files, file operations and web search. And this task operations and web search. And this task operations and web search. And this task tool basically means it will launch a tool basically means it will launch a tool basically means it will launch a new agent for keywords and file search. new agent for keywords and file search. new agent for keywords and file search. When cloud code agent is calling this When cloud code agent is calling this When cloud code agent is calling this task tool, it basically is creating task tool, it basically is creating task tool, it basically is creating another agent that has almost all the another agent that has almost all the another agent that has almost all the tool except those planning related tools tool except those planning related tools tool except those planning related tools like task to-do and this agent will like task to-do and this agent will like task to-do and this agent will receive a well-defined tasks from the receive a well-defined tasks from the receive a well-defined tasks from the parent agent do a list of things and in parent agent do a list of things and in parent agent do a list of things and in the end say here are the findings. Only the end say here are the findings. Only the end say here are the findings. Only the last part which is summary of the the last part which is summary of the the last part which is summary of the findings will be sent back to the parent findings will be sent back to the parent findings will be sent back to the parent cloud code agent. And this is one cloud code agent. And this is one cloud code agent. And this is one message they have to really save the message they have to really save the message they have to really save the token consumption for the parent agent token consumption for the parent agent token consumption for the parent agent because otherwise the main agent might because otherwise the main agent might because otherwise the main agent might be flooded with all sorts of context. be flooded with all sorts of context. be flooded with all sorts of context. And knowing this, you can actually And knowing this, you can actually And knowing this, you can actually utilize this task to a lot better. For utilize this task to a lot better. For utilize this task to a lot better. For example, you can actually prompt cloud example, you can actually prompt cloud example, you can actually prompt cloud code to use task two to set up multiple code to use task two to set up multiple code to use task two to set up multiple parallel agent to do different task at parallel agent to do different task at parallel agent to do different task at the same time. But also if there's a the same time. But also if there's a the same time. But also if there's a task that you know already, it's going task that you know already, it's going task that you know already, it's going to read some very large files. Try to to read some very large files. Try to to read some very large files. Try to prompt cloud code to just use task two prompt cloud code to just use task two prompt cloud code to just use task two to do this because that will help you to do this because that will help you to do this because that will help you save the token a lot on the main agent. save the token a lot on the main agent. save the token a lot on the main agent. So now I finished the planning and come So now I finished the planning and come So now I finished the planning and come back with a plan about what are the back with a plan about what are the back with a plan about what are the layout system should look like file layout system should look like file layout system should look like file explorer which makes sense the code explorer which makes sense the code explorer which makes sense the code editor component it decide to use editor component it decide to use editor component it decide to use existing library and then it will break existing library and then it will break existing library and then it will break down into different phases. The phase down into different phases. The phase down into different phases. The phase one is just install everything and the one is just install everything and the one is just install everything and the phase two is implement file explorer phase two is implement file explorer phase two is implement file explorer code editor terminal integration and code editor terminal integration and code editor terminal integration and some advanced feature as well as the some advanced feature as well as the some advanced feature as well as the project directory plan. So this is a project directory plan. So this is a project directory plan. So this is a pretty good plan and obviously I can pretty good plan and obviously I can pretty good plan and obviously I can keep planning but here I can also ask it keep planning but here I can also ask it keep planning but here I can also ask it to start do this plan and next is that to start do this plan and next is that to start do this plan and next is that it will create this detailed it will create this detailed it will create this detailed implementation plan incloud/tasks implementation plan incloud/tasks implementation plan incloud/tasks and I will click yes. So it will create and I will click yes. So it will create and I will click yes. So it will create this task folder and create this online this task folder and create this online this task folder and create this online ID front end with a detailed plan and ID front end with a detailed plan and ID front end with a detailed plan and tasks. So now I can ask it to let's do tasks. So now I can ask it to let's do tasks. So now I can ask it to let's do phase one. It will create a to-do based phase one. It will create a to-do based phase one. It will create a to-do based on the phase one requirements. So while on the phase one requirements. So while on the phase one requirements. So while it is running I'm just going to quickly it is running I'm just going to quickly it is running I'm just going to quickly talk about this to-do. So I was quite talk about this to-do. So I was quite talk about this to-do. So I was quite curious how did cloud code actually curious how did cloud code actually curious how did cloud code actually handle this to-do. Do they handling to a handle this to-do. Do they handling to a handle this to-do. Do they handling to a different like planner agent to just different like planner agent to just different like planner agent to just specifically come up with plan and specifically come up with plan and specifically come up with plan and programmatically get the agent to do one programmatically get the agent to do one programmatically get the agent to do one task of another? So I did some task of another? So I did some task of another? So I did some investigation. What I found is the investigation. What I found is the investigation. What I found is the entropy team actually took the the entropy team actually took the the entropy team actually took the the simplest setup possible. Uh but it's simplest setup possible. Uh but it's simplest setup possible. Uh but it's really effective. It has this tool really effective. It has this tool really effective. It has this tool called to-do right description is called to-do right description is called to-do right description is basically use this tool to create and basically use this tool to create and basically use this tool to create and manage and structure task list. They manage and structure task list. They manage and structure task list. They have a very specific prompt talk about have a very specific prompt talk about have a very specific prompt talk about like when to use this tool, when to not like when to use this tool, when to not like when to use this tool, when to not use this tool, some example as well. So use this tool, some example as well. So use this tool, some example as well. So every time when agent run this tool, it every time when agent run this tool, it every time when agent run this tool, it will try to come up a list to-do and will try to come up a list to-do and will try to come up a list to-do and each to-do will have the content ID, each to-do will have the content ID, each to-do will have the content ID, priority and status. It is as simple as priority and status. It is as simple as priority and status. It is as simple as this. So now if we come back here it this. So now if we come back here it this. So now if we come back here it finish all the to-do it also going back finish all the to-do it also going back finish all the to-do it also going back to our doc and adding details about what to our doc and adding details about what to our doc and adding details about what it actually did. So it marked this as it actually did. So it marked this as it actually did. So it marked this as completed and also document all the completed and also document all the completed and also document all the things that it does. Cool. So it has things that it does. Cool. So it has things that it does. Cool. So it has this component implemented that it has this component implemented that it has this component implemented that it has this file explorer resizable. It has this file explorer resizable. It has this file explorer resizable. It has terminal. I can add multiple different terminal. I can add multiple different terminal. I can add multiple different terminal as well. Nice. Obviously you terminal as well. Nice. Obviously you terminal as well. Nice. Obviously you can see I can continue going because it can see I can continue going because it can see I can continue going because it already have trace of the overall plan. already have trace of the overall plan. already have trace of the overall plan. So every time it just need to focus on So every time it just need to focus on So every time it just need to focus on one specific piece of work and if there one specific piece of work and if there one specific piece of work and if there any time when the plan change we can any time when the plan change we can any time when the plan change we can just prompt it to update the stock. So just prompt it to update the stock. So just prompt it to update the stock. So this is kind my like doc or spec focused this is kind my like doc or spec focused this is kind my like doc or spec focused workflow with cloud code. But what's workflow with cloud code. But what's workflow with cloud code. But what's really cool about cloud code is that you really cool about cloud code is that you really cool about cloud code is that you can customize cloud code in very deep can customize cloud code in very deep can customize cloud code in very deep manner. So one super interesting feature manner. So one super interesting feature manner. So one super interesting feature is hooks. So hook is a feature that is hooks. So hook is a feature that is hooks. So hook is a feature that allow you to define things to happen allow you to define things to happen allow you to define things to happen programmatically when cloud code takes programmatically when cloud code takes programmatically when cloud code takes certain actions. One of the most basic certain actions. One of the most basic certain actions. One of the most basic and common hook that I use is this stop and common hook that I use is this stop and common hook that I use is this stop hook. So I can define a rule here when hook. So I can define a rule here when hook. So I can define a rule here when cloud code stop which means it finish cloud code stop which means it finish cloud code stop which means it finish the task it try to run this command the task it try to run this command the task it try to run this command which will basically play the system which will basically play the system which will basically play the system sound to notify me that the task has sound to notify me that the task has sound to notify me that the task has been completed. So with this one if I been completed. So with this one if I been completed. So with this one if I just send a message to cloud it'll play just send a message to cloud it'll play just send a message to cloud it'll play this notification sound after the task this notification sound after the task this notification sound after the task is finished. So this is basically simple is finished. So this is basically simple is finished. So this is basically simple concept of hook but interesting thing is concept of hook but interesting thing is concept of hook but interesting thing is that the customization here can go that the customization here can go that the customization here can go pretty deep. Cloud code allow you to pretty deep. Cloud code allow you to pretty deep. Cloud code allow you to define things to do before or after define things to do before or after define things to do before or after certain tool is wrong or when user try certain tool is wrong or when user try certain tool is wrong or when user try to send a new message to cloud code so to send a new message to cloud code so to send a new message to cloud code so that you can ingest some additional that you can ingest some additional that you can ingest some additional context in as well as when cloud try to context in as well as when cloud try to context in as well as when cloud try to compress the conversation history or compress the conversation history or compress the conversation history or when a sub agent finishes task and when a sub agent finishes task and when a sub agent finishes task and here's a more sophisticated example that here's a more sophisticated example that here's a more sophisticated example that I found actually pretty useful. So I can I found actually pretty useful. So I can I found actually pretty useful. So I can define this post to use hook that every define this post to use hook that every define this post to use hook that every time after cloud code run a edit time after cloud code run a edit time after cloud code run a edit multi-edit or write to which means it multi-edit or write to which means it multi-edit or write to which means it modify or create some new files. It will modify or create some new files. It will modify or create some new files. It will run this Python file I define here run this Python file I define here run this Python file I define here called type check. So this is a feature called type check. So this is a feature called type check. So this is a feature that I probably missed the most from that I probably missed the most from that I probably missed the most from cursor is that in cursor it has this cursor is that in cursor it has this cursor is that in cursor it has this automatic linked arrow detection and automatic linked arrow detection and automatic linked arrow detection and this is really useful context so that this is really useful context so that this is really useful context so that cursor can capture those errors even cursor can capture those errors even cursor can capture those errors even before you run the code and proactively before you run the code and proactively before you run the code and proactively fix issue. But for cloud it didn't have fix issue. But for cloud it didn't have fix issue. But for cloud it didn't have this and this hook will basically this and this hook will basically this and this hook will basically replicate that functionality directly. replicate that functionality directly. replicate that functionality directly. So inside this Python file it might look So inside this Python file it might look So inside this Python file it might look a bit complicated but I will quickly a bit complicated but I will quickly a bit complicated but I will quickly explain to you what that means. So we explain to you what that means. So we explain to you what that means. So we firstly try to get input data. Each hook firstly try to get input data. Each hook firstly try to get input data. Each hook comes with a list of different inputs comes with a list of different inputs comes with a list of different inputs that you can use. For example for the that you can use. For example for the that you can use. For example for the post to use input it will automatically post to use input it will automatically post to use input it will automatically give you a list data that you can use in give you a list data that you can use in give you a list data that you can use in this like which tool it is what are the this like which tool it is what are the this like which tool it is what are the input agent generate for this tool and input agent generate for this tool and input agent generate for this tool and what's the output of tool. And in our what's the output of tool. And in our what's the output of tool. And in our case, we want to get the file so we know case, we want to get the file so we know case, we want to get the file so we know which file agent just created or which file agent just created or which file agent just created or modified. Then we'll get a file pass. modified. Then we'll get a file pass. modified. Then we'll get a file pass. And if this file extension is ts or tsx, And if this file extension is ts or tsx, And if this file extension is ts or tsx, which means it's typescript, we will run which means it's typescript, we will run which means it's typescript, we will run the type check. And if this type check the type check. And if this type check the type check. And if this type check failed, this is a part where we can send failed, this is a part where we can send failed, this is a part where we can send feedback back to cloud code. So you will feedback back to cloud code. So you will feedback back to cloud code. So you will do the print and the file will be system do the print and the file will be system do the print and the file will be system std error. Here I define code to be two. std error. Here I define code to be two. std error. Here I define code to be two. So code two means that it is a blocking So code two means that it is a blocking So code two means that it is a blocking arrow and std arrow will be fed back to arrow and std arrow will be fed back to arrow and std arrow will be fed back to cloud code so that it can use that to cloud code so that it can use that to cloud code so that it can use that to define the next actions. But you can define the next actions. But you can define the next actions. But you can also define other asset code that will also define other asset code that will also define other asset code that will still send message back to cloud code still send message back to cloud code still send message back to cloud code but it won't blocking it from continue but it won't blocking it from continue but it won't blocking it from continue the next action. So with this hook if the next action. So with this hook if the next action. So with this hook if cloud code generates on files that has cloud code generates on files that has cloud code generates on files that has type errors it will automatically call type errors it will automatically call type errors it will automatically call the script and return back a error the script and return back a error the script and return back a error message if it has any type. So cloud message if it has any type. So cloud message if it has any type. So cloud code can try to proactively fix issue. code can try to proactively fix issue. code can try to proactively fix issue. So this is how hook works and there are So this is how hook works and there are So this is how hook works and there are a lot of potentials. For example, you a lot of potentials. For example, you a lot of potentials. For example, you can even define some critic agent. After can even define some critic agent. After can even define some critic agent. After cloud code write any code, it can write cloud code write any code, it can write cloud code write any code, it can write a test and validate it or if it write a test and validate it or if it write a test and validate it or if it write API doc, you can automatically update API doc, you can automatically update API doc, you can automatically update documentations. I have included a few documentations. I have included a few documentations. I have included a few common hooks that I use a lot in AI common hooks that I use a lot in AI common hooks that I use a lot in AI builder club. So you can go and grab. If builder club. So you can go and grab. If builder club. So you can go and grab. If you guys want a more deep dive or hook you guys want a more deep dive or hook you guys want a more deep dive or hook feature, please comment below, let me feature, please comment below, let me feature, please comment below, let me know. I'm happy to do another one. On know. I'm happy to do another one. On know. I'm happy to do another one. On the outside, cloud code also have this the outside, cloud code also have this the outside, cloud code also have this commands feature where they do come with commands feature where they do come with commands feature where they do come with a list of predefined command like check a list of predefined command like check a list of predefined command like check cost, set up MCP, memory, models and cost, set up MCP, memory, models and cost, set up MCP, memory, models and review PR commands even, but they also review PR commands even, but they also review PR commands even, but they also allow you to define custom slash allow you to define custom slash allow you to define custom slash commands. For example, I can create a commands. For example, I can create a commands. For example, I can create a commands folder under cloud and just commands folder under cloud and just commands folder under cloud and just create something called joke MD and make create something called joke MD and make create something called joke MD and make a joke in all caps. Once I did this, I a joke in all caps. Once I did this, I a joke in all caps. Once I did this, I can do slash and search for joke. Then can do slash and search for joke. Then can do slash and search for joke. Then this command I define here will be this command I define here will be this command I define here will be showing up. If I click on that, whatever showing up. If I click on that, whatever showing up. If I click on that, whatever you define here in this command will you define here in this command will you define here in this command will basically be sent to cloud code almost basically be sent to cloud code almost basically be sent to cloud code almost as a prompt. Network start behave based as a prompt. Network start behave based as a prompt. Network start behave based on the rules you define there. I have on the rules you define there. I have on the rules you define there. I have another video where I talk about another video where I talk about another video where I talk about different commands that I predefined different commands that I predefined different commands that I predefined that can help you extract specific that can help you extract specific that can help you extract specific styling from a screenshots as well as styling from a screenshots as well as styling from a screenshots as well as getting cloud code into design mode to getting cloud code into design mode to getting cloud code into design mode to design multiple different UI iterations. design multiple different UI iterations. design multiple different UI iterations. And you can even have a command to get a And you can even have a command to get a And you can even have a command to get a cloud code to set up multiple different cloud code to set up multiple different cloud code to set up multiple different git work tree to have sub agents working git work tree to have sub agents working git work tree to have sub agents working in parallel. So you can go check out in parallel. So you can go check out in parallel. So you can go check out this video if you want to learn more. this video if you want to learn more. this video if you want to learn more. And there's a one package I thought is And there's a one package I thought is And there's a one package I thought is pretty useful recently called pretty useful recently called pretty useful recently called supercloud. It's a open source package supercloud. It's a open source package supercloud. It's a open source package that comes with bunch of commands that that comes with bunch of commands that that comes with bunch of commands that they predefine and build. For example, I they predefine and build. For example, I they predefine and build. For example, I can do slash command SC analyze. This can do slash command SC analyze. This can do slash command SC analyze. This will trigger that command that take will trigger that command that take will trigger that command that take cloud into a much deeper code analyze cloud into a much deeper code analyze cloud into a much deeper code analyze mode. It will create list of to-do to mode. It will create list of to-do to mode. It will create list of to-do to look through the whole codebase and come look through the whole codebase and come look through the whole codebase and come back with a kind of architecture review. back with a kind of architecture review. back with a kind of architecture review. There are other useful things like you There are other useful things like you There are other useful things like you can run workflow to look at a PRD doc to can run workflow to look at a PRD doc to can run workflow to look at a PRD doc to get cloud into kind of step-by-step get cloud into kind of step-by-step get cloud into kind of step-by-step implementation process as well as build implementation process as well as build implementation process as well as build command that will help bundle and npm command that will help bundle and npm command that will help bundle and npm build your projects as well as build your projects as well as build your projects as well as troubleshoot if you have some weird bug troubleshoot if you have some weird bug troubleshoot if you have some weird bug that you don't know how to fix. So this that you don't know how to fix. So this that you don't know how to fix. So this is a really useful package even though is a really useful package even though is a really useful package even though the installation is not that the installation is not that the installation is not that straightforward. We need to open in a straightforward. We need to open in a straightforward. We need to open in a folder do uv in it first to set up a folder do uv in it first to set up a folder do uv in it first to set up a python project and then do uv as python project and then do uv as python project and then do uv as supercloud. This will add this supercloud. This will add this supercloud. This will add this supercloud package and in the end do UV supercloud package and in the end do UV supercloud package and in the end do UV run Python supercloud install then it run Python supercloud install then it run Python supercloud install then it will take you through the step-by-step will take you through the step-by-step will take you through the step-by-step installation process but once you installation process but once you installation process but once you install it will be installed at global install it will be installed at global install it will be installed at global level so you don't need to keep doing level so you don't need to keep doing level so you don't need to keep doing this and all the file will be basically this and all the file will be basically this and all the file will be basically saved to your docloud settings at user saved to your docloud settings at user saved to your docloud settings at user level if we open it it will basically level if we open it it will basically level if we open it it will basically add stuff into the cloud MD which link add stuff into the cloud MD which link add stuff into the cloud MD which link to all the different files that contain to all the different files that contain to all the different files that contain more details inside this command folder more details inside this command folder more details inside this command folder you can see all the commands that you you can see all the commands that you you can see all the commands that you define here so this kind of quick define here so this kind of quick define here so this kind of quick example of how far the customization you example of how far the customization you example of how far the customization you can do with just command feature. can do with just command feature. can do with just command feature. Meanwhile, the list of very useful Meanwhile, the list of very useful Meanwhile, the list of very useful feature and shortcuts that cloud code feature and shortcuts that cloud code feature and shortcuts that cloud code has that I didn't know initially. has that I didn't know initially. has that I didn't know initially. Firstly, I can do slash resume to Firstly, I can do slash resume to Firstly, I can do slash resume to jumping back to a past conversation jumping back to a past conversation jumping back to a past conversation history and continue the conversation history and continue the conversation history and continue the conversation there. This is kind of similar to cursor there. This is kind of similar to cursor there. This is kind of similar to cursor where you can choose a past where you can choose a past where you can choose a past conversation. But on the other hand, conversation. But on the other hand, conversation. But on the other hand, they also introduces export command. So they also introduces export command. So they also introduces export command. So this feature will allow you to copy the this feature will allow you to copy the this feature will allow you to copy the whole conversation history with cloud whole conversation history with cloud whole conversation history with cloud code. So you can jump between cloud code. So you can jump between cloud code. So you can jump between cloud code, cursor, werf or any other coding code, cursor, werf or any other coding code, cursor, werf or any other coding IDE because you just need to paste in IDE because you just need to paste in IDE because you just need to paste in conversation history and no need to conversation history and no need to conversation history and no need to worry that cursor don't have the context worry that cursor don't have the context worry that cursor don't have the context about what has been done and quite often about what has been done and quite often about what has been done and quite often I will also go to cloud where I have I will also go to cloud where I have I will also go to cloud where I have unlimited amount token to do some early unlimited amount token to do some early unlimited amount token to do some early exploration. Apart from that another exploration. Apart from that another exploration. Apart from that another really useful feature is that you can really useful feature is that you can really useful feature is that you can double tap exit and this will allow you double tap exit and this will allow you double tap exit and this will allow you to revert back conversation history to a to revert back conversation history to a to revert back conversation history to a past point. So you can just click that past point. So you can just click that past point. So you can just click that and continue the conversation from that and continue the conversation from that and continue the conversation from that point. This really good because point. This really good because point. This really good because sometimes cloud code will make a sometimes cloud code will make a sometimes cloud code will make a mistakes and this can avoid it. The only mistakes and this can avoid it. The only mistakes and this can avoid it. The only downside is that if you're in cursor downside is that if you're in cursor downside is that if you're in cursor when revert back to a past conversation when revert back to a past conversation when revert back to a past conversation it will automatically revert back all it will automatically revert back all it will automatically revert back all your files as well but cloud code didn't your files as well but cloud code didn't your files as well but cloud code didn't keep a snapshot. So you actually need to keep a snapshot. So you actually need to keep a snapshot. So you actually need to use some external package to do the use some external package to do the use some external package to do the snapshot and versioning. One of them is snapshot and versioning. One of them is snapshot and versioning. One of them is called CC undo. So this is a package called CC undo. So this is a package called CC undo. So this is a package that will automatically detect all the that will automatically detect all the that will automatically detect all the changes that cloud code has made to your changes that cloud code has made to your changes that cloud code has made to your file and allow it to roll back. So I can file and allow it to roll back. So I can file and allow it to roll back. So I can do CC undo and the list. This will list do CC undo and the list. This will list do CC undo and the list. This will list out all the change that your cloud code out all the change that your cloud code out all the change that your cloud code has been made and you can do CC undo has been made and you can do CC undo has been made and you can do CC undo preview to see the specific change that preview to see the specific change that preview to see the specific change that cloud code has been made and then once cloud code has been made and then once cloud code has been made and then once you confirm you can do CC undo undo and you confirm you can do CC undo undo and you confirm you can do CC undo undo and select a change you want to revert. So select a change you want to revert. So select a change you want to revert. So this package can work with cloud code this package can work with cloud code this package can work with cloud code handing pretty well. Though the command handing pretty well. Though the command handing pretty well. Though the command line might not be the best and easiest line might not be the best and easiest line might not be the best and easiest way for you to preview the work. There way for you to preview the work. There way for you to preview the work. There are more userfriendly version like yo-yo are more userfriendly version like yo-yo are more userfriendly version like yo-yo where it receive a snapshot and you can where it receive a snapshot and you can where it receive a snapshot and you can add some additional instructions to add some additional instructions to add some additional instructions to remember what the changes are. Another remember what the changes are. Another remember what the changes are. Another quite useful shortcut is that you can quite useful shortcut is that you can quite useful shortcut is that you can type in acceleration mark. And this will type in acceleration mark. And this will type in acceleration mark. And this will get the cloud code into bash mode. So get the cloud code into bash mode. So get the cloud code into bash mode. So bash mode allow you to run command bash mode allow you to run command bash mode allow you to run command directly without going outside cloud directly without going outside cloud directly without going outside cloud code. So I can do quickshot car like code. So I can do quickshot car like code. So I can do quickshot car like pmppm install or pmppm add certain pmppm install or pmppm add certain pmppm install or pmppm add certain package and it will just run the command package and it will just run the command package and it will just run the command directly here. So it is really fast but directly here. So it is really fast but directly here. So it is really fast but also the more important part is that also the more important part is that also the more important part is that this context will be part of this context will be part of this context will be part of conversation history so that cloud code conversation history so that cloud code conversation history so that cloud code will know what are the actions that will know what are the actions that will know what are the actions that you've been taken and similarly you can you've been taken and similarly you can you've been taken and similarly you can also do hashtag which will activate the also do hashtag which will activate the also do hashtag which will activate the memory mode. So here is where you can memory mode. So here is where you can memory mode. So here is where you can type in things that you want cloud code type in things that you want cloud code type in things that you want cloud code to memorize like I'm JSON we're building to memorize like I'm JSON we're building to memorize like I'm JSON we're building online ID using chassis and component online ID using chassis and component online ID using chassis and component and then it will ask you to choose where and then it will ask you to choose where and then it will ask you to choose where do you want to save this memory it can do you want to save this memory it can do you want to save this memory it can be per level or can be user level across be per level or can be user level across be per level or can be user level across all the projects and once you confirm it all the projects and once you confirm it all the projects and once you confirm it just save that information to the just save that information to the just save that information to the cloudme and the last part I also want to cloudme and the last part I also want to cloudme and the last part I also want to show you the easiest way for you to show you the easiest way for you to show you the easiest way for you to connect cloud code to kim K2 model if connect cloud code to kim K2 model if connect cloud code to kim K2 model if you don't know Kim K2 model is a new you don't know Kim K2 model is a new you don't know Kim K2 model is a new open-source coding model that has open-source coding model that has open-source coding model that has similar performance like cloud 3.5 to similar performance like cloud 3.5 to similar performance like cloud 3.5 to cloud four but 80% cheaper. So it is cloud four but 80% cheaper. So it is cloud four but 80% cheaper. So it is really good one to experiment if you run really good one to experiment if you run really good one to experiment if you run out of credits. And the easiest way to out of credits. And the easiest way to out of credits. And the easiest way to set up is that you can open terminal and set up is that you can open terminal and set up is that you can open terminal and do code open this zshrc file. If you're do code open this zshrc file. If you're do code open this zshrc file. If you're on Mac like me, you probably just do on Mac like me, you probably just do on Mac like me, you probably just do this. But if you're on Windows, you this. But if you're on Windows, you this. But if you're on Windows, you might need to change to bash. This will might need to change to bash. This will might need to change to bash. This will open a file like this. So this will open a file like this. So this will open a file like this. So this will basically control your terminal basically control your terminal basically control your terminal behavior. So here I will define Kimi API behavior. So here I will define Kimi API behavior. So here I will define Kimi API key. And don't worry, I already disabled key. And don't worry, I already disabled key. And don't worry, I already disabled this key. So it's not going to work for this key. So it's not going to work for this key. So it's not going to work for you guys anymore. And I can define kimi you guys anymore. And I can define kimi you guys anymore. And I can define kimi where it will export entropic base URL where it will export entropic base URL where it will export entropic base URL to moonshot using this API key and run to moonshot using this API key and run to moonshot using this API key and run cloud. So with this one I can just go to cloud. So with this one I can just go to cloud. So with this one I can just go to my any terminal and then do kimi. This my any terminal and then do kimi. This my any terminal and then do kimi. This will open cloud code with the special will open cloud code with the special will open cloud code with the special model that I defined there. So if I type model that I defined there. So if I type model that I defined there. So if I type in hi here it is actually going to talk in hi here it is actually going to talk in hi here it is actually going to talk to the kimi k2 model. And similarly you to the kimi k2 model. And similarly you to the kimi k2 model. And similarly you can also define things here like claw can also define things here like claw can also define things here like claw bash maintain per working directory to bash maintain per working directory to bash maintain per working directory to be one. What this will do is that be one. What this will do is that be one. What this will do is that basically every time when cloud is basically every time when cloud is basically every time when cloud is running, it will always append the running, it will always append the running, it will always append the current working directory into the current working directory into the current working directory into the prompt. So it will always remember where prompt. So it will always remember where prompt. So it will always remember where it is instead of running command in run it is instead of running command in run it is instead of running command in run place. So here's a quick overview of how place. So here's a quick overview of how place. So here's a quick overview of how I use cloud code. If you want to learn I use cloud code. If you want to learn I use cloud code. If you want to learn more, you can join AI builder club where more, you can join AI builder club where more, you can join AI builder club where we have weekly sessions to talk through we have weekly sessions to talk through we have weekly sessions to talk through the latest workflow and tips for AI the latest workflow and tips for AI the latest workflow and tips for AI coding and build large language models coding and build large language models coding and build large language models as well as all detailed rules, hooks and as well as all detailed rules, hooks and as well as all detailed rules, hooks and commands that I personally use. So you commands that I personally use. So you commands that I personally use. So you can copy paste directly. In upcoming can copy paste directly. In upcoming can copy paste directly. In upcoming weeks, we'll have more detailed weeks, we'll have more detailed weeks, we'll have more detailed breakdown of how does a cloud code breakdown of how does a cloud code breakdown of how does a cloud code actually work behind the scenes and try actually work behind the scenes and try actually work behind the scenes and try to rebuild cloud code from scratch so to rebuild cloud code from scratch so to rebuild cloud code from scratch so that we can learn the best practice of that we can learn the best practice of that we can learn the best practice of building effective AI coding agents. I building effective AI coding agents. I building effective AI coding agents. I put a link in the description below for put a link in the description below for put a link in the description below for you to join. I hope you enjoy this you to join. I hope you enjoy this you to join. I hope you enjoy this video. Thank you and I see you next video. Thank you and I see you next video. Thank you and I see you next time.
